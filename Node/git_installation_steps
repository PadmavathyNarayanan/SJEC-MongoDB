//To download git in windows
https://git-scm.com/downloads

//Verify git version in command prompt
git --version

If any error encountered on executing above command,check if git.exe got added in environment path variable
https://stackoverflow.com/a/26620861

//To generate personalised tokens
https://github.com/settings/tokens
Tokens(classic) -> click on Generate new token -> Generate new token(classic) ->On scopes select repo,workflow,admin:repo_hook -> Click on Generate token
 Then, Copy and save the token locally.

//git commands
git status
git fetch
git add --all
git commit -m "<commit message>"
git remote add origin https://<TOKEN>@github.com/<REPO-OWNER-USERNAME>/<REPO-NAME>
git push
